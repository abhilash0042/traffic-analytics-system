# Environment & secrets
.env
venv/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Data & training artifacts (large — download/train locally)
data/
runs/
models/*.pt

# Pretrained weights (auto-downloaded by Ultralytics)
weights/*.pt
*.pt

# Pipeline outputs
output/*
!output/.gitkeep
output_video.mp4
results_log.json

# Kaggle pack artifacts (upload zip manually — too large for git)
dist/*.zip
dist/*.log
dist/*.err
balance_*.txt
balance_*.log
balance_*.err
repair_balance.*
restore.*
finish_balance.log

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
notebooks/output_helmant
output/analysis_frames/

# Optional: keep PDFs local only (extracted text is in docs/paper_texts/)
docs/papers/*.pdf
